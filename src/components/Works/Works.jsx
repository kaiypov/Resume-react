import React from 'react';

const Works = () => {
    return (
        <div>
            <h2>Unfortunately, this page does not load, try to come back later!</h2>
        </div>
    );
};

export default Works;